{
  "name": "today-fishing-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "selfcheck": "node --experimental-strip-types selfcheck.mts",
    "gen-points": "node --experimental-strip-types scripts/gen-points.mts"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "~5.7.2",
    "wrangler": "^4.112.0"
  }
}
